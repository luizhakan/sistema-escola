import { ValidacaoDirective } from './validacao.directive';

describe('ValidacaoDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidacaoDirective();
    expect(directive).toBeTruthy();
  });
});
